apiVersion: metallb.io/v1beta1
kind: IPAddressPool
metadata:
  name: {{ .Values.config.poolName }}
  namespace: {{ .Release.Namespace }}
spec:
  addresses:
    - {{ .Values.config.ipRange }}
  autoAssign: {{ .Values.config.autoAssign }}

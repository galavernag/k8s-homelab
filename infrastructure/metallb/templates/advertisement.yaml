apiVersion: metallb.io/v1beta1
kind: L2Advertisement
metadata:
  name: { { .Values.config.advertisementName } }
  namespace: { { .Release.Namespace } }
spec:
  ipAddressPools:
    - { { .Values.config.poolName } }
